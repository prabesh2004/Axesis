# Frontend -> Backend wiring
#
# Set this to your backend base URL (no trailing slash)
# Example: http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000

# Keep true until backend is running.
# When you're ready to use the real backend, set to false.
VITE_USE_MOCK_API=true

# Google OAuth (for login)
# Create an OAuth Client ID (Web application) in Google Cloud Console.
# Authorized JS origins should include your dev frontend, e.g. http://localhost:8080
VITE_GOOGLE_CLIENT_ID=
