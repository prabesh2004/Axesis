# Server
PORT=3000
NODE_ENV=development

# MongoDB (Atlas or local)
MONGODB_URI=mongodb://127.0.0.1:27017/ai-knowledge-hub

# JWT
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d

# CORS
# Frontend dev server port is configured in vite.config.ts
# You can pass a comma-separated list of allowed origins.
CORS_ORIGIN=http://localhost:8081

# Groq API
GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=llama-3.1-8b-instant

# Google OAuth (for login)
# Must match the OAuth Client ID used by the frontend.
GOOGLE_CLIENT_ID=
